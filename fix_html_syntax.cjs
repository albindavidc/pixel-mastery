const fs = require('fs');
let content = fs.readFileSync('src/components/HtmlPlayground.tsx', 'utf8');

// I might have replaced something wrong. Let's just fix it by writing the file from scratch carefully.
